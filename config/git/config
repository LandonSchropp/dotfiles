[credential]
  helper = osxkeychain

[user]
  name = Landon Schropp
  email = schroppl@gmail.com

[push]
  default = current
  followTags = true
  autoSetupRemote = true

[pull]
  ff = only

[alias]

  # Shortcuts
  a = add
  b = branch
  bs = branches
  c = commit
  cl = clone
  co = checkout-with-block
  cp = cherry-pick
  fb = feature-branch
  fp = force-push
  fpu = force-pull
  fu = fixup
  l = compact-log
  m = merge
  my-bs = my-branches
  p = push --follow-tags
  pu = pull
  r = reset
  rb = !git fetch && git rebase-base --autosquash
  re = restore
  ri = !git fetch && git rebase-base -i
  s = status
  sc = smart-commit
  sq = squash
  sw = smart-switch
  ta = town append
  tb = town branch
  td = town delete
  tp = town prepend
  ts = town sync

  # Command aliases
  aliases = "config --get-regexp alias"
  amend = "commit --amend"
  compact-log = log \

  # Command aliases
  aliases = "config --get-regexp alias"
  amend = "commit --amend"
  compact-log = log \
    --graph \
    --pretty=format:'%Cred%h %Cgreen(%cs)%Creset%Creset %s %C(bold blue)<%an>%C(yellow)%d%Creset' \
    --abbrev-commit
  current-branch = "rev-parse --abbrev-ref HEAD"
  dad = "!curl https://icanhazdadjoke.com/ && echo"
  discard = git restore --staged --worktree
  squash = "fixup --squash"
  my-branches = "!git branches | grep -i '$()'"
  unstage = restore --staged 
  stash-staged = stash push --staged

  # Custom script aliases
  base-branch = "!$HOME/.config/git/bin/base-branch"
  branches = "!$HOME/.config/git/bin/branches"
  checkout-with-block = "!$HOME/.config/git/bin/block-checkout"
  default-branch = "!$HOME/.config/git/bin/default-branch"
  default-branch = "!$HOME/.config/git/bin/default-branch"
  delete-all-branches = "!$HOME/.config/git/bin/delete-branches"
  delete-merged-branches = "!$HOME/.config/git/bin/delete-branches --merged"
  feature-branch = "!$HOME/.config/git/bin/feature-branch"
  force-pull = "!$HOME/.config/git/bin/force-pull"
  force-push = "!$HOME/.config/git/bin/force-push"
  my-branches = "!$HOME/.config/git/bin/my-branches"
  rebase-base = "!$HOME/.config/git/bin/rebase-base"
  review = "!$HOME/.config/git/bin/review"
  smart-commit = "!$HOME/.config/git/bin/smart-commit"
  smart-switch = "!$HOME/.config/git/bin/smart-switch"

[core]
  excludesfile = "~/.config/git/ignore"
  mergeoptions = "--no-edit"
  pager = "diff-so-fancy | less --tabs=2 -R"

[interactive]
  diffFilter = "diff-so-fancy --patch"

[fetch]
  prune = true

[rebase]
  autosquash = true
	empty = drop
	updateRefs = true

[diff]
  colorMoved = zebra

[status]
  showUntrackedFiles = all

[color "diff"]
  meta = "yellow bold"
  commit = "green bold"
  frag = "magenta bold"
  old = "red bold"
  new = "green bold"
  whitespace = "red reverse"

[color "diff-highlight"]
  oldNormal = "red bold"
  oldHighlight = "black bold red"
  newNormal = "green bold"
  newHighlight = "black bold green"

[merge]
  tool = vimdiff
  ff = only

[merge "rails-schema-resolver"]
	name = Rails db/schema.rb version conflict resolver
	driver = ~/.config/git/bin/resolve-rails-schema-conflict %O %A %B %L %P

[rerere]
  enabled = true

[log]
  abbrevCommit = true

[init]
  defaultBranch = main

[fixup]
  menu = fzf \
    --bind 'tab:toggle-preview' \
    --preview 'git show --color {+1}' \
    --preview-window=up:50% \
    --info hidden \
    --prompt '🔍 Search for a commit: '
  commit = true
  rebase = true

[filter "lfs"]
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
