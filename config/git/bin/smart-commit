#!/usr/bin/env bash

set -euo pipefail

# Generate commit message using claude
commit_message=$(claude --print "/git:commit-message")

# Create commit with generated message but allow editing
git commit --message "$commit_message" --edit
