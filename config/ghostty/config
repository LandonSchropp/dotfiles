# Set the font to my preferences
font-family = "JetBrainsMono Nerd Font Mono"
font-size = 19
font-style = Medium

# Disable ligatures
font-feature = -calt, -liga, -dlig

# Set the theme to Tokyo Night Storm
theme = "tokyonight-storm"

# Increase the window padding so the characters don't bump up against the edge
window-padding-x = 4

# Don't display the resize overlay
resize-overlay = never

# Increase the line height
adjust-cell-height = 25%

# Automatically download updates
auto-update = download

# Override the default cursor style
cursor-style = "block"
shell-integration-features = no-cursor

# Add more accessible keybindings for tmux commands. The `\x13` string is the `control+s` ASDII
# character.
#
# TODO: Figure out how to run the tmux commands directly rather than triggering tmux keybindings.
keybind = command+h=text:\x13p
keybind = command+l=text:\x13n
keybind = command+m=text:\x13s

# Make alt keys behave normally
macos-option-as-alt = false
