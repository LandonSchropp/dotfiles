-- Heirline is way more trouble than it's worth. Because everything is super custom, there's almost
-- no configuration options available. Instead, I'm opting to use purpose-built plugins for this
-- functionality instead.
return {
  "rebelot/heirline.nvim",
}
