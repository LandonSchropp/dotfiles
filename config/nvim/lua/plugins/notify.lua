-- Don't delete this! This is necessary to disable nvim-notify—using the `notifications = false` in
-- the astrocore configuration is insufficient by itself.
return {
  "rcarriga/nvim-notify",
  enabled = false,
}

