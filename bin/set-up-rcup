#!/usr/bin/env bash

set -euo pipefail

hostname="$(hostname -s)"

if [[ "$hostname" == OHR* ]]; then
  tag="work"
else
  tag="personal"
fi

rcup \
  -t "$tag" \
  -v \
  -U Library \
  Library \
  claude \
  config \
  local \
  tmux.conf \
  zprofile \
  zshenv \
  zshrc
