#!/usr/bin/env bash

set -euo pipefail

hostname="$(hostname -s)"

if [[ "$hostname" == OHR* ]]; then
  tag="oyster"
else
  tag="personal"
fi

rcup \
  -t "$tag" \
  -v \
  -x config/window-manager \
  -U Library \
  Library \
  claude \
  config \
  tmux.conf \
  zprofile \
  zshenv \
  zshrc
