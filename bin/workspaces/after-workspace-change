#!/usr/bin/env bash

set -euo pipefail

# Reset the Phoenix layout (Command + Option + Control + Shift + R)
osascript <<'END'
tell application "System Events"
  keystroke "r" using {command down, option down, control down}
end tell
END
